<form method="post" action="{{ path('app_dinosaure_delete', {'id': dinosaure.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dinosaure.id) }}">
    <button class="btn">Delete</button>
</form>
