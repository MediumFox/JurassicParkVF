{% extends 'base.html.twig' %}

{% block title %}Horaire{% endblock %}

{% block body %}
    <h1>Horaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horaire.id }}</td>
            </tr>
            <tr>
                <th>Ouverture</th>
                <td>{{ horaire.ouverture ? horaire.ouverture|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Fermeture</th>
                <td>{{ horaire.fermeture ? horaire.fermeture|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_horaire_index') }}">back to list</a>

    <a href="{{ path('app_horaire_edit', {'id': horaire.id}) }}">edit</a>

    {{ include('horaire/_delete_form.html.twig') }}
{% endblock %}
