<form method="post" action="{{ path('app_format_chambre_delete', {'id': format_chambre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ format_chambre.id) }}">
    <button class="btn">Delete</button>
</form>
