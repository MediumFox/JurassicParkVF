{% extends 'base.html.twig' %}

{% block title %}Enclos{% endblock %}

{% block body %}
    <h1>Enclos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enclo.id }}</td>
            </tr>
            <tr>
                <th>LibelleEnclos</th>
                <td>{{ enclo.libelleEnclos }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enclos_index') }}">back to list</a>

    <a href="{{ path('app_enclos_edit', {'id': enclo.id}) }}">edit</a>

    {{ include('enclos/_delete_form.html.twig') }}
{% endblock %}
